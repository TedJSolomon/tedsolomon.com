export const metadata = {
  title: 'Meeting Cost Calculator — Ted Solomon',
  description: 'See what your meetings really cost. A real-time cost ticker with per-second updates and attendee breakdowns.',
};

export default function MeetingCalculatorLayout({ children }) {
  return children;
}
